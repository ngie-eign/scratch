#!/bin/sh

SRCCONF=${SRCCONF:=/etc/src.conf}

for i in $(make -f "$SRCCONF" -V KERNCONF); do
    make installkernel \
        KERNCONF=$i \
        INSTKERNNAME=$i.r$(svn info | awk '/^Revision/ {print $2}')${SUFFIX:+.$SUFFIX} \
	$*
done
